ultralytics>=8.3,<9
opencv-python>=4.10
screeninfo>=0.8
numpy>=1.26
lap>=0.5.12
pygame>=2.6
PyOpenGL>=3.1
openvino>=2024.0,<2027
onnx>=1.16,<2
yt-dlp>=2024.8
# Optional — Edge YouTube cookie export on Windows (run_youtube_test.bat):
# rookiepy
# Optional — only needed to (re)export Depth Anything V2 Small OpenVINO IR:
# transformers>=4.40,<5
# huggingface_hub>=0.23
# torch  # already pulled by ultralytics
